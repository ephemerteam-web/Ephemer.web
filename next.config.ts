import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Configuration pour next-intl
  experimental: {
    // Permettre l'utilisation de middleware avec i18n
  },
};

export default nextConfig;
